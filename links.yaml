- source: dotfiles
  dest: ''
  dot: true
- source: bin
  dest: .local/bin
  shallow: true
- source: config
  dest: config
  shallow: true
- source: dotfiles
  dest: /root
  dot: true
  sudo: true
  only:
    - dircolors
    - zshrc
    - zshenv
- source: config
  dest: /root/.config
  sudo: true
  shallow: true
  only:
    - bat
    - nvim
    - git
